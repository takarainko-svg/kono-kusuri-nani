# 薬剤データ仕様 v1

対象ファイルは `drugs.json`、スキーマは `drugs.schema.json` です。

## 基本方針

- 1レコードは原則として1つの一般名・有効成分単位で管理する。
- 商品名は別レコードにせず `brands` に登録する。
- 英語名、慣用名、検索補助語は `aliases` に登録する。
- 薬剤情報の一次情報源はPMDA電子添文とする。
- 表現はケアマネが生活上の変化に気づき、医療職へつなぐための要約とする。
- 診断、処方、服薬変更・中止の判断を促す表現は使用しない。

## 必須項目

- `id`: 英小文字・数字・ハイフンで一意にする。
- `name`: 画面で主に表示する一般名。
- `generic`: 正式な一般名・成分名。
- `brands`: 代表的・実務上検索されやすい商品名。後発品のメーカー別名称を網羅しない。
- `aliases`: 英語名など検索補助用の別名。
- `className`: 薬効分類。
- `what`: 「何の薬か」。1〜2文で平易に説明する。
- `use`: 主な適応・用途。
- `common`: 主な副作用。現場で気づきやすいものを優先する。
- `serious`: 見逃したくない副作用。重大な変化をケアマネが認識しやすい表現に要約する。
- `caution`: 服薬上の主な注意。
- `care`: ケアマネとして観察するポイント。診断ではなく、生活上の変化を捉える表現にする。
- `source.type`: 現行仕様では `PMDA` 固定。
- `source.url`: 参照したPMDA電子添文のURL。
- `source.revision`: 参照した電子添文の改訂時期。
- `source.checkedAt`: 実際に情報を確認した日（Asia/Tokyo、YYYY-MM-DD）。
- `record.createdAt`: 初回登録日。
- `record.updatedAt`: 内容を最後に変更した日。
- `record.version`: 内容変更のたびに1増やす。

## 更新ルール

- PMDAを再確認し、内容変更がない場合は `source.checkedAt` のみ更新する。
- 改訂時期だけ変わった場合は `source.revision` と `source.checkedAt` を更新する。
- 要約内容を変更した場合は `record.updatedAt` を更新し、`record.version` を1増やす。
- 新規追加時は `record.createdAt` と `record.updatedAt` を同日にし、`record.version` は1とする。
- 商品名で追加依頼を受けた場合は、既存一般名レコードの `brands` に該当しないか先に確認する。

## 文章ルール

- 「何の薬か」は専門用語より役割を先に書く。
- 主な副作用は原則3〜8項目を目安とする。
- `care` は「低Na血症を確認」ではなく「強いだるさ、ぼんやり、食欲低下など普段と違う変化」のように生活場面で観察可能な表現を優先する。
- 因果関係を断定せず、「変化に気づいて医療職へつなぐ」ための表現にする。
